[package]
name = "analog-remote-view-capture"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cross-platform Factorio window capture abstraction"

[dependencies]
analog-remote-view-core = { workspace = true }
sysinfo = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[target.'cfg(target_os = "macos")'.dependencies]
# ScreenCaptureKit bindings — used for window enumeration / capture on macOS.
screencapturekit = "10"
