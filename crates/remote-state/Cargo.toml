[package]
name = "analog-remote-view-remote-state"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Remote View state abstraction (marker now, IPC later)"

[dependencies]
analog-remote-view-core = { workspace = true }
